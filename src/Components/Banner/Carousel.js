import { styled } from '@mui/material/styles';
import React from 'react'

const AppRoot = styled('div')(({ theme }) => ({
    height: "50%",
    display: "flex",
    alignItems: "center",
}));


const Carousel = () => {
    const fetchTrendingCoins = () =>{
        
    }
  return (
    <AppRoot>Carousel</AppRoot>
  )
}

    export default Carousel
